//Variable gloabale
let compteur =0
let timer,elements,slide
window.onload=()=>{
    const diapo =document.querySelector(".diapo")
    elements = document.querySelector(".elements")
    slide = Array.from(elements.children)
     
}